%this function calculate the probability of multi dimension gaussian of
%each region using reg.mu and reg.vars.
%ps(1,m) the m dimensional point.
function [p] = calc_trustprob_gaussian( reg, pt )
p = multigaussian_prob( reg.mu, reg.vars, pt );
